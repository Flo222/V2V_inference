# RoCooper

RoCooper's model, fusion and native communication/aggregation components are grouped here. `rocooper_comm.py` is baseline-native behavior; the shared Markov background for fair cross-baseline experiments belongs in `opencood.communication.channel`.
